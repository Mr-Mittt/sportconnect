# Session Module — Feature Backlog

**Version:** MVP v1
**Module:** `modules/session/session-impl`
**Last updated:** 2026-09-24 (SESSION-43 done)

---

## How to use this file

- Pick the first `TODO` ticket in the implementation order
- Mark it `IN PROGRESS` at the start of the session
- Mark it `DONE` when implementation + tests are complete
- Use `/feature <ticket-id>` to plan, `/implement` to execute

---

## Open (TODO / IN PROGRESS)

| # | Ticket | Title | Status |
|---|---|---|---|
| 1 | [SESSION-8](MVP/SESSION-8_SESSION_DISCOVER_RANKING_ALGORITHM.md) | Session discover ranking algorithm | `TODO` |
| 2 | [SESSION-22](MVP/SESSION-22_FLAKY_SESSION_EVENTS_CONSUMER_RABBITMQ_IT.md) | `SessionEventsConsumerIntegrationTest` fails intermittently on its RabbitMQ container — **~50% flake rate** (6 of 12 full runs), fails as a block on `AmqpIOException`, passes in isolation | `TODO` |
| 3 | [SESSION-26](MVP/SESSION-26_DISCOVER_ATTRIBUTE_FILTER.md) | Session-attribute filtering on discover (deferred from SESSION-25) | `TODO` |
| 4 | [SESSION-29](MVP/SESSION-29_OLD_HISTORY_STORAGE_RETENTION_CONCERN.md) | Old `CANCELLED`/`COMPLETED` session storage — retention concern, documented only, no direction decided (revisit once there's real usage/storage data) | `TODO` |
| 5 | [SESSION-41](MVP/SESSION-41_GENERATION_FAILURE_BACKSTOP.md) | No retry/backstop for a failed eager group-session generation attempt — SESSION-38's failure isolation leaves no periodic sweep to catch a missed group | `TODO` |
| 6 | [SESSION-44](MVP/SESSION-44_REJECT_JOIN_AND_APPROVAL_ON_COMPLETED_SESSION.md) | `joinSession` (and approve/reject) reject only `CANCELLED`, not `COMPLETED` — a join or join request on a finished session succeeds (and notifies the creator); found during client CLIENT-SESSION-30 by code review, not yet reproduced live | `TODO` |

---

## Done

| # | Ticket | Title | Status |
|---|---|---|---|
| 1 | [SESSION-43](MVP/SESSION-43_UPCOMING_AND_HISTORY_SPORTID_FILTER.md) | `sportId` on `GET /sessions/upcoming` (**optional** — the all-sports rail needs one unfiltered call) and `GET /sessions/history` (**required**, both the `date=` list and `dateCount=` counts shapes; `sport_id` is NOT NULL) — plain equality, deliberately **no** active-sport-profile gate (unlike `/discover`), since these are scoped by the caller's own participant rows. Unblocks client CLIENT-SESSION-23 (filed + done 2026-09-24; live-checked against real Postgres) | `DONE` (2026-09-24) |
| 2 | [SESSION-42](MVP/SESSION-42_DISCOVER_PARTICIPANT_EXCLUSION_AND_REQUESTED_ENDPOINT.md) | Discover's caller-exclusion widened from `JOINED`-only to also exclude `REQUESTED`/`INVITED` (`findDiscoverSessions`/`findDiscoverDateCounts` gained a new `excludedParticipantStatuses` param, kept separate from the unchanged `joinedStatus` capacity-counting param). New `GET /api/sessions/requested` — "my pending requests," `REQUESTED`-only across `PREPARING`/`SCHEDULED`/`ONGOING`, standalone or group-linked — needed zero new repository code (`findUpcomingSessions` was already generic over `participantStatuses`, reused with `List.of(REQUESTED)`). **Scope correction at pickup:** status filter widened from the originally filed `PREPARING/SCHEDULED` to include `ONGOING` (matching `/upcoming`) after verifying no code path clears a `REQUESTED` row when a session starts; a stale `REQUESTED` row surviving into `CANCELLED`/`COMPLETED` is a documented, accepted known gap. Client `e2e/mocks/handlers/sessions.ts`'s `/discover` MSW handler widened inline in the same change. Green: `session-impl` full Spock suite (23 existing call-arg assertions updated, 2 new tests) + `SessionDiscoverIntegrationTest`/`SessionDiscoverDateCountsIntegrationTest`/`SessionListingIntegrationTest` (89 tests combined, new REQUESTED/INVITED + `/requested` coverage) + `:server:test` (full suite, 0 failures) + client `tsc -b` clean + `matches-journey.spec.ts` e2e re-verified | `DONE` (2026-09-22) |
| 3 | [SESSION-40](MVP/SESSION-40_GETSESSION_MISSING_VISIBILITY_GATE.md) | `GET /api/sessions/{sessionId}` was ungated entirely — any authenticated user could read any session's full details, including a private group's. New `SessionDetailGate implements ResourceGate<Session>` (kept separate from `SessionGate`, which gates comments/likes with a different rule): visible if `isPublic`, or (standalone) caller holds a `JOINED`/`REQUESTED`/`INVITED` participant row, or (group-linked) caller is a group member; availability-gated on the parent group still being active. **Scope change at pickup (user decision): `getGroupSessions` brought into scope** (originally out of scope) and tightened to member-only regardless of the group's own public/private flag — closes a real gap where a *public* group's sessions were listable by any authenticated user, not just members. Consumer census: no client flow depended on non-member access to either endpoint (Discover's standalone `isPublic=true` flow stays fully open; `useGroupSessionsForGroups` only ever queries the caller's own groups). Green: `session-impl` full Spock suite (new `SessionDetailGateSpec`, 11 cases) + new `SessionAccessGateIntegrationTest` (13 real `MockMvc`+H2 cases) + `:server:test` (full suite, 0 failures) | `DONE` (2026-09-22) |
| 4 | [SESSION-39](MVP/SESSION-39_SESSION_COUNT_ENDPOINT.md) | New `GET /api/sessions/discover/counts` endpoint — per-date counts of `discoverSessions`-shaped results across a date list (capped at 8) or the default today+7-days window, no pagination, backfilled to 0 for any date with no matches. Native `findDiscoverDateCounts` query (the ADR's benchmarked "Query C" shape — `AT TIME ZONE`+`TO_CHAR`+ordinal `GROUP BY 1`, combined with `discoverSessions`' own `MOD`-based `startTimeFilter` mechanism, kept deliberately over an unverified direct `AT TIME ZONE` extraction). **Scope change at pickup (user decision): `locationId` became a multi-value, OR-combined filter** (`List<Long>`, repeated param) — originally unlike `/discover`'s single-value exact match, but **`/discover` was widened to match the same day (post-completion addendum, user-directed)**; `sportId` stays single-value on both. Also strengthened the `isPublic`-gate tests (here and in `SessionDiscoverIntegrationTest`) with a public group-linked fixture, proving the gate is genuinely `is_public` and not `group_id IS NULL`. Re-verified the ADR's Query C re: the new `idx_sessions_sport_id_standalone` index (SESSION-37) against real dev Postgres, seeded/rolled-back: `sport_id = <single>` uses the index directly (0.92ms); `sport_id IN (<3>)` falls back to a different index with a post-filter (1.97ms) — both fast, confirms the ADR's finding transfers. Client follow-up already tracked via `CLIENT-SESSION-25`'s own Delta (updated with the shipped contract), not a new ticket. Green: `session-impl` (full Spock suite) + both discover IT classes in isolation (47 tests) + `:server:test` (full suite, 0 failures) | `DONE` (2026-09-22) |
| 5 | [SESSION-38](MVP/SESSION-38_EVENT_DRIVEN_GROUP_SESSION_GENERATION.md) | Replaced the hourly full-group-scan sweep with three direct triggers (session completion, `updateGroupRecurrence`, and — scope added at pickup — `updateGroupSettings`) sharing one extracted `generateForConfigs`; new `group-api` batch method + `session-api` method, `group-impl` depends on `session-api` for the first time (`@Lazy` cycle fix, same precedent as `postService`). Found and fixed 2 real pre-existing production bugs via this ticket's own IT coverage: auto-generated sessions never created their required companion `SESSION_POST` (NOT NULL violation silently masked as a benign race since V051/SESSION-10 shipped), and a caught exception inside a Spring participating transaction still poisons it (fixed with `REQUIRES_NEW`). Filed **SESSION-41** for the resulting no-retry-backstop gap. **Follow-up IT pass (2026-09-22, user request — now in CLAUDE.md's Testing section):** closed 4 more IT gaps (settings-trigger happy path, idempotency, multi-group batching, a new failure-isolation test) — the failure-isolation test immediately found a *third, deeper* instance of the transaction-poisoning bug (`generateNextOccurrenceForGroup` was itself `@Transactional`, re-poisoning the caller one level up); fixed by dropping that annotation. Green: `session-impl` + `group-impl` (full Spock suites) + `:server:test` (full suite, 262 tests, 0 failures) | `DONE` (2026-09-21) |
| 6 | [SESSION-37](MVP/SESSION-37_DISCOVER_OPEN_ENDED_DATE_FLOOR.md) | New `Session.isPublic` column (replaces `groupId IS NULL` as Discover's predicate) + `idx_sessions_sport_id_standalone`'s predicate swap; `/discover` reverts to a single required `date` with a smart today/future floor (a past date silently clamps to today), `startTimeFilter`/`startTime` independently optional, `ONGOING` drops out of the default status list (explicit `ONGOING` stripped, not a 400), page size default `20`→`10`. Split `SessionCount` off to **SESSION-39**; found and filed **SESSION-40** (`getSession` visibility gap) and client **CLIENT-SESSION-26** (Discover has no "load more" pagination). Green: `session-impl` (full Spock suite) + `:server:test` (full suite, 0 failures) + live-verified `V071`/index swap against real dev Postgres | `DONE` (2026-09-21) |
| 7 | [SESSION-36](MVP/SESSION-36_FLAKY_COMPUTENEXTOCCURRENCE_TIME_OF_DAY_TESTS.md) | `SessionGenerationServiceSpec`'s `computeNextOccurrence` tests derived expectations from `LocalTime.now()`/`LocalDate.now()` at test-run time — `LocalTime.now() ± N hours` wraps around midnight (e.g. `00:30 - 1h = 23:30`), silently inverting the intended before/after comparison. **Fixed test-only, no production change**: a `Clock`-injection design was drafted and traced through in discussion, then rejected — the bug is entirely in how the tests construct their fixtures, not in `computeNextOccurrence`'s own logic (always called with a real, fixed recurrence time in production). Final fix: replaced the arithmetic with fixed boundary constants (`LocalTime.MIDNIGHT`/`LocalTime.MAX`, never after/before any real `now()` for the entire day) — deterministic for 100% of real run times. Green: `session-impl` (full module suite) + `:server:test` (confirmed unaffected via `UP-TO-DATE`, prior full green run of 248 tests stands) | `DONE` (2026-09-19) |
| 8 | [SESSION-35](MVP/SESSION-35_DISCOVER_CALLER_ZONE_FILTERS.md) | `discoverSessions`'s `date`/`startTime` filters, `getUpcomingSessions`'s `date` filter, and `getSessionHistory`'s `date` filter all now evaluate against a caller-supplied `viewerZoneId` (optional IANA zone id, `ZoneId.of(...)`-validated, falls back to `"UTC"` when omitted) instead of the JVM's own zone — a single `resolveZone` helper backs all four. `date`'s day-boundary fix needed no query change (a plain `Instant` range); `startTime` keeps its pre-existing `EXTRACT`+offset-shift correction, now sourced from the caller's zone instead of the JVM's (a real per-row `AT TIME ZONE` rewrite was evaluated and rejected — HQL has no such operator, and its Postgres function-call passthrough fails on H2; see `SessionRepository.findDiscoverSessions`'s Javadoc for the accepted residual DST-across-seasons gap this leaves). **Scope grew mid-implementation** (user call) to also fix `/upcoming?date` and `/history?date`'s identical JVM-zone bug in the same ticket. Consumer census: no live client caller of `/upcoming` or `/history?date` exists yet, but `CLIENT-SESSION-24`'s scope was extended to send `viewerZoneId` to both once `CLIENT-SESSION-23` wires them up. **Scope grew again same day (user call): `/discover`'s `date` made required** (was optional since SESSION-25) — consumer census found a real, live client consumer (`useDiscoverSessions.ts`, CLIENT-SESSION-6's Discover panel/modal) that omits `date` entirely; user chose to proceed and fix the client too, accepting the resulting "today only" narrowing (follow-up **CLIENT-SESSION-25** filed for a real date picker) and the loss of `startTimeFilter`'s "any day" search mode (ANDed with the now-required `date`). "Do we have enough IT?" found 2 real gaps (startTimeFilter's date-exclusion and date's own exclusion were never directly proven) — both fixed. Green: `session-impl` (173 tests) + `SessionDiscoverIntegrationTest` (30 tests) + `SessionListingIntegrationTest` (existing, real Postgres) + client `tsc`/Vitest/E2E green; full `:server:test` blocked by an unrelated Docker/Hyper-V outage on the dev machine | `DONE` (2026-09-18) |
| 9 | [SESSION-34](MVP/SESSION-34_HISTORY_DATE_COUNTS_AT_TIME_ZONE.md) | `findHistoryDateCounts` rewritten to bucket by `COALESCE(origin_zone_id, location.timezone)` via an explicit `AT TIME ZONE` conversion (new `LEFT JOIN locations`), retiring the reverted `zoneOffsetSeconds` point-fix — supersedes SESSION-31. A row with no resolvable zone (routine until CLIENT-SESSION-24 ships) is excluded from bucketing and logged at `WARN`, never mis-bucketed or a hard error. Found and worked around a real H2-vs-Postgres divergence: H2's `CAST(timestamptz AS date)` re-normalizes through the session zone instead of preserving `AT TIME ZONE`'s shifted fields — fixed via `TO_CHAR`, verified identical on both engines (H2 + a real-Postgres temp-table reproduction). Green: `session-impl` (164 tests) + `:server:test` (235 tests) | `DONE` (2026-09-17) |
| 10 | [SESSION-33](MVP/SESSION-33_SCHEDULEDSTART_TRUE_INSTANT.md) | `Session.scheduledStart`/`scheduledEndAt` become true instants (`TIMESTAMPTZ`), with a creator-zone `originZoneId` fallback (pinned forever, per user decision) for location-less sessions — foundation for SESSION-34/35. Intentional, non-additive contract break on `scheduledStart`'s wire shape (client must send an offset now); `SessionGenerationService` gained a `LocationService` dependency to resolve auto-generated sessions' zone from their (always-present) recurrence location. `/discover`'s `startTimeFilter` correction left mechanically type-compatible but not truly zone-correct yet — re-verified live (27 cases green), SESSION-35's job to fix properly. Green: `session-impl` (162 tests) + `:server:test` (231 tests) | `DONE` (2026-09-17) |
| 11 | [SESSION-32](MVP/SESSION-32_LOCATION_TIMEZONE_DESIGN.md) | Sessions have no client/location timezone — `scheduledStart` implicitly server-zone; design captured in `documentation/md/LOCATION_TIMEZONE_DESIGN.md` (location-owned zones, `scheduledStart` as a true instant, location-zone-for-bucketing vs. caller-zone-for-filtering split, validated `TIMESTAMP`/`TIMESTAMPTZ` example against real Postgres) and split into concrete tickets: **LOC-4**, **SESSION-33**, **SESSION-34**, **SESSION-35**, **CLIENT-SESSION-24** | `DONE` (2026-09-16) |
| 12 | [SESSION-31](MVP/SESSION-31_HISTORY_DATE_COUNTS_TIMEZONE_BUG.md) | `findHistoryDateCounts` misbuckets early-morning sessions onto the wrong calendar date (real, already-shipped timezone bug found while implementing SESSION-25) — a JVM-offset-correction point-fix was implemented and fully verified (H2 + real Postgres, `:server:test` green), then **deliberately reverted before merge** once a follow-up discussion chose a root-cause redesign instead (location-owned timezones, `scheduledStart` as a true instant) over patching this one query. Superseded by **SESSION-34** | `SUPERSEDED` (2026-09-16) |
| 13 | [SESSION-25](MVP/SESSION-25_DISCOVER_SEARCH_AND_FILTER.md) | Server-side search + column filter on session discover — 8 new AND-combined query params on `GET /discover` (title, locationId, minOpenSlots, feeType, maxFeeAmountVnd, date, startTimeFilter+startTime, status), new default status list (`PREPARING`/`SCHEDULED`/`ONGOING`, superseding SESSION-4's `SCHEDULED`-only default — intentional, user-confirmed), `scheduledStart >= now()` default lower bound (skipped when `date`/`startTimeFilter` narrows it), new 3-level sort (`scheduledStart ASC` + open-slots `ASC` + `createdAt ASC`). Adding a permanent IT test (`SessionDiscoverIntegrationTest`, prompted by "do we have enough IT?") found two real bugs no mocked Spock test or `:server:test` H2 run could have caught: (1) a bare `(:param IS NULL OR ...)` null check fails Postgres's parameter-type inference — fixed by casting every optional param defensively; (2) a systemic **timezone bug** — this app's `hibernate.jdbc.time_zone: UTC` shifts stored timestamps on write but only reapplies that shift on plain attribute reads, so any `CAST`/`EXTRACT` applied directly to `scheduledStart` reads the raw, wrong value (confirmed: an `18:00`-local session read back as hour `11` via `EXTRACT`; an early-morning session's `CAST(... AS date)` returned the previous calendar day). Fixed `date` via a half-open `[dayStart, dayEnd)` range (no cast, matching SESSION-27's existing pattern) and `startTime` via `EXTRACT` + a JVM-offset `MOD` correction computed in `SessionServiceImpl` — three earlier attempts (native `time`-typed comparison, string comparison, naively-shifted parameter) each failed a different way before landing on this. Same bug found **already shipped** in SESSION-27's `findHistoryDateCounts` — not fixed here (different ticket), filed as **SESSION-31**. **SESSION-30** (the original CAST-vs-generated-column performance question) is now superseded — the correctness fix made the performance question moot. Green: session-impl (expanded `discoverSessions` Spock) + `SessionDiscoverIntegrationTest` (27 IT cases, H2) + `:server:test` (full suite) + live end-to-end re-verification of every boundary on real Postgres, including the two bugs' exact repro cases | `DONE` (2026-09-15) |
| 14 | [SESSION-30](MVP/SESSION-30_DISCOVER_DATE_TIME_FILTER_GENERATED_COLUMNS.md) | `/discover`'s date/startTime filters: CAST-vs-generated-column performance question — **superseded**: CAST turned out to be a correctness bug (see SESSION-25), not just a performance tradeoff, so neither option applies anymore | `SUPERSEDED` (2026-09-15) |
| 15 | [SESSION-28](MVP/SESSION-28_SESSION_PARTICIPANTS_USER_STATUS_INDEX.md) | Index cleanup, two parts — Part 1 drops redundant `idx_sessions_group_id`/`idx_session_participants_session_id` (`V066`, superseded by existing unique composite indexes); Part 2 adds `session_participants(user_id) WHERE status='JOINED'` (`V067`, partial index — covers 5 of 7 "my sessions"-shaped query consumers directly, `getUpcomingSessions`/`ByDate`'s `INVITED` half correctly falls back to the existing plain `user_id` index). Both verified via `EXPLAIN` on the real dev Postgres. `idx_sessions_created_by`/`idx_sessions_location_id` reviewed, kept not dropped. Green: session-impl + `:server:test`, no Java changed (index-only) | `DONE` (2026-09-15) |
| 16 | [SESSION-27](MVP/SESSION-27_REFACTOR_SESSION_LISTING_UPCOMING_AND_HISTORY.md) | Refactor session listing — replaces `GET /sessions/mine` with `GET /upcoming` (JOINED/INVITED, PREPARING/SCHEDULED/ONGOING, scheduledStart ASC + PREPARING→SCHEDULED→ONGOING tiebreak — scope change at pickup) and `GET /history` (`date`: JOINED-only CANCELLED/COMPLETED, scheduledStart DESC; `dateCount`: distinct history dates + counts, `before` cursor). Both participant-scoped (standalone + group-linked alike), sort non-overridable by the caller. No migration. Green: session-impl (11 new Spock) + new `SessionListingIntegrationTest` (19 IT cases, real H2 round trip) + `:server:test` (195, all passed). Client wiring deferred to already-filed `CLIENT-SESSION-23` | `DONE` (2026-09-15) |
| 17 | [SESSION-24](MVP/SESSION-24_ADD_PREPARING_SESSION_STATUS.md) | Add `PREPARING` session status — `locationId`/`feeType` optional at creation; missing either starts the session `PREPARING` instead of `SCHEDULED`; `updateSession` gains a `PREPARING`-only gate on those two fields plus the completion flip; new `cancelUnpreparedSessions` job auto-cancels a `PREPARING` session past its `scheduledStart`; new `session.details.updated` notification (JOINED participants, any field change) — routing key renamed from the originally-planned `session.updated` after finding `notification-impl`'s queue binds a strict `session.*.*` pattern that a 2-segment key would silently never match. `V065` drops `NOT NULL` on both `sessions.location_id` and `.fee_type`. Green: session-impl (148) + notification-impl + V065 on dev Postgres; `:server:test` not run to completion (~55min, no output — matches the precedent below), deferred to `server-ci` on push | `DONE` (2026-09-14) |
| 18 | [SESSION-23](MVP/SESSION-23_SESSION_ATTRIBUTES.md) | Session attributes (2026-09-07) — `V064` `sessions.attributes` JSONB; `attributes` on create/update requests + `SessionResponse`; `SessionAttributeFilter` (+ cloned `SessionAttributeValues`/`SessionSchemaPaths`) filters submitted maps against `SportService.getSessionAttributeSchemaRaw` with **replace semantics**, 4KB cap → 400; `updateSession` skips the schema fetch when `attributes` is null. Value-validation logic cloned from `sport-impl` (unreachable package-private) — de-dup filed as sport `A23` + common `C5`. Green: session-impl + `:server:test` (179) + V064 on dev Postgres | `DONE` (2026-09-07) |
| 19 | [SESSION-21](MVP/SESSION-21_SYSTEM_COMMENTS_IN_SESSION_THREAD.md) | System comments in the session discussion thread | `DONE` |
| 20 | [SESSION-20](MVP/SESSION-20_COMMENT_NOTIFICATION_STATUS_GATE_BUG.md) | Comment notifications wrongly restricted to SCHEDULED/ONGOING sessions | `DONE` |
| 21 | [SESSION-19](MVP/SESSION-19_NOTIFY_JOINED_PARTICIPANTS_ON_LEAVE.md) | Notify JOINED participants when a participant leaves | `DONE` |
| 22 | [SESSION-18](MVP/SESSION-18_NOTIFY_JOINED_PARTICIPANTS_WHEN_A_SESSION_TRANSITIONS_TO.md) | Notify JOINED participants when a session transitions to ONGOING | `DONE` |
| 23 | [SESSION-16](MVP/SESSION-16_FIX_JOINSESSION_DEMOTING_AN_ALREADY_JOINED_CALLER_BACK.md) | Fix `joinSession` demoting an already-`JOINED` caller back to `REQUESTED` | `DONE` |
| 24 | [SESSION-17](MVP/SESSION-17_OUTBOX_PENDING_PARTIAL_INDEX.md) | Partial index on `session_outbox_events` scoped to `status = 'PENDING'` | `DONE` |
| 25 | [SESSION-14](MVP/SESSION-14_REDUCE_MAPTORESPONSES_ROUND_TRIPS.md) | Reduce `mapToResponses`' round trips (2 points) | `DONE` |
| 26 | [SESSION-15](MVP/SESSION-15_NOTIFICATION_OUTBOX_WIRING.md) | Notification outbox wiring — closes NOTIF-1 | `DONE` |
| 27 | [SESSION-10](MVP/SESSION-10_SESSION_POST_COMMENTS.md) | Session comments — reuses post-impl's Comment via a companion `SESSION_POST` anchor | `DONE` |
| 28 | [SESSION-12](MVP/SESSION-12_PARTIAL_SCHEDULED_STATUS_INDEX.md) | Partial index on `sessions` scoped to `status = SCHEDULED` for the generation job's hot queries | `DONE` |
| 29 | [SESSION-13](MVP/SESSION-13_SESSIONRESPONSE_LIKECOUNT_ISLIKEDBYCURRENTUSER_POSTSERVICE_G.md) | `SessionResponse.likeCount`/`isLikedByCurrentUser` + `PostService.getSessionPostLikeInfo` batch method | `DONE` |
| 30 | [SESSION-11](MVP/SESSION-11_DROP_CROSS_DOMAIN_FKS.md) | Drop DB-level FKs on session tables' cross-domain columns | `DONE` |
| 31 | [SESSION-9](MVP/SESSION-9_CALLER_PARTICIPATION_STATUS.md) | Expose the caller's own participant status (any status) via getSessionParticipants | `DONE` |
| 32 | [SESSION-4](MVP/SESSION-4_STANDALONE_DISCOVERY.md) | Standalone session discovery — browse/join sessions you didn't create | `DONE` |
| 33 | [SESSION-5](MVP/SESSION-5_CAPACITY_AND_FEE.md) | Session capacity + fee/pricing | `DONE` |
| 34 | [SESSION-6](MVP/SESSION-6_JOIN_APPROVAL_AND_INVITES.md) | Join-approval workflow + invite-friends-at-creation | `DONE` |
| 35 | [SESSION-7](MVP/SESSION-7_PARTIAL_INDEX_ON_SESSIONS_SPORT_ID.md) | Partial index on `sessions.sport_id` for standalone sport filtering | `DONE (bundled into SESSION-4)` |
| 36 | [SESSION-1](MVP/SESSION-1_SESSION_DOMAIN_CORE.md) | Session domain core — manual create/join/leave, group or standalone | `DONE` |
| 37 | [SESSION-2](MVP/SESSION-2_SCHEDULED_AUTO_GENERATION_JOB.md) | Scheduled auto-generation job for group-recurring sessions | `DONE` |
| 38 | [SESSION-3](MVP/SESSION-3_FULL_STATUS_LIFECYCLE.md) | Full status lifecycle (ONGOING, CANCELLED) + cancel reason/who/when | `DONE` |
