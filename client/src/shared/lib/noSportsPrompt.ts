/** Shared copy for the zero-sport-profile "add a sport first" gate (CLIENT-SESSION-7 follow-up)
 * — the page-access trigger on Groups/Matches uses this exact wording on both pages, unlike the
 * create/join gates inside CreateSessionModal/SessionDiscoverModal, which are action-specific and
 * define their own message locally. */
export const PAGE_ACCESS_NO_SPORTS_PROMPT =
  "Hey champ, add a sport first — this page won't make much sense without one! 🏅";
