# Production Profile Configuration
spring:
  datasource:
    # ?options=-c%20TimeZone%3DUTC — defensive only; pgjdbc overrides it with its own "SET TIME
    # ZONE <JVM default>" after connecting, so the real fix is TimeZone.setDefault("UTC") in
    # SportConnectApplication.main() (applies here too — see documentation/md/
    # JDBC_SESSION_TIMEZONE_FIX.md). Found via a dev-host bug (host OS timezone -> Java
    # "Asia/Bangkok"), kept here too since prod's container OS timezone is an external assumption,
    # not a guarantee.
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}?options=-c%20TimeZone%3DUTC
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10
  
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
  
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}

  rabbitmq:
    host: ${RABBITMQ_HOST}
    port: ${RABBITMQ_PORT}
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}

# Production Server
server:
  port: ${SERVER_PORT:8080}

# Production Logging
logging:
  level:
    root: WARN
    com.sportconnect: INFO
    org.springframework.web: WARN
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
  file:
    name: /var/log/sportconnect/application.log

# Production JWT (must be set via environment variables)
app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: 86400000 # 24 hours
    refresh-expiration: 604800000 # 7 days

  # Gates /internal/** — services/chat's cold-start bootstrap pull. See application.yml's comment.
  internal-service-secret: ${INTERNAL_SERVICE_SECRET}

  # NTF-3: STOMP broker-relay target — see application.yml's comment for why login/passcode reuse
  # spring.rabbitmq's own credentials.
  stomp-relay:
    host: ${STOMP_RELAY_HOST:${spring.rabbitmq.host}}
    port: ${STOMP_RELAY_PORT:61613}
